@page
@model Airport.Soft.Areas.AirportsFlight.Pages.AirportsFlights.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>AirportsFlightData</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="AirportsFlightData.FlightId" />
            <input type="hidden" asp-for="AirportsFlightData.AirportId" />
            <div class="form-group">
                <label asp-for="AirportsFlightData.Id" class="control-label"></label>
                <input asp-for="AirportsFlightData.Id" class="form-control" />
                <span asp-validation-for="AirportsFlightData.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
